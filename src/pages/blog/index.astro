---
import Layout from '../../layouts/Layout.astro';

// Blog posts are authored on Medium and linked here.
// Add your posts to this array — or automate via GitHub Actions.
const posts: {
  title: string;
  date: string;
  summary: string;
  url: string;
  readingTime?: string;
  tags?: string[];
}[] = [
  // Example (remove when you add real posts):
  // {
  //   title: 'My first post',
  //   date: '2025-01-15',
  //   summary: 'A short description of what the post covers.',
  //   url: 'https://medium.com/@merwan-birem/my-first-post',
  //   readingTime: '5 min',
  //   tags: ['research', 'engineering'],
  // },
];
---

<Layout title="Blog" description="Writing by Merwan Birem — research notes, engineering deep-dives, and reflections.">

  <div class="mb-12">
    <h1 class="text-2xl font-semibold tracking-tight mb-2">Blog</h1>
    <p class="text-[var(--color-muted)]">
      Writing hosted on{' '}
      <a href="https://medium.com" target="_blank" rel="noopener noreferrer" class="text-[var(--color-accent)] hover:underline">
        Medium
      </a>.
      Research notes, engineering deep-dives, and reflections.
    </p>
  </div>

  {posts.length === 0 ? (
    <p class="text-[var(--color-muted)] text-sm italic">Posts coming soon.</p>
  ) : (
    <ul class="space-y-10">
      {posts.map(post => (
        <li>
          <a href={post.url} target="_blank" rel="noopener noreferrer" class="group block">
            <div class="flex items-center gap-3 mb-2">
              <time class="text-xs text-[var(--color-faint)] font-mono">
                {new Date(post.date).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' })}
              </time>
              {post.readingTime && (
                <span class="text-xs text-[var(--color-faint)]">{post.readingTime} read</span>
              )}
            </div>
            <h2 class="text-base font-medium group-hover:text-[var(--color-accent)] transition-colors mb-1">
              {post.title}
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" class="inline w-3 h-3 ml-1 opacity-40 group-hover:opacity-100 transition-opacity">
                <path d="M6.22 8.72a.75.75 0 0 0 1.06 1.06l5.22-5.22v1.69a.75.75 0 0 0 1.5 0v-3.5a.75.75 0 0 0-.75-.75h-3.5a.75.75 0 0 0 0 1.5h1.69L6.22 8.72Z" />
                <path d="M3.5 6.75c0-.69.56-1.25 1.25-1.25H7A.75.75 0 0 0 7 4H4.75A2.75 2.75 0 0 0 2 6.75v4.5A2.75 2.75 0 0 0 4.75 14h4.5A2.75 2.75 0 0 0 12 11.25V9a.75.75 0 0 0-1.5 0v2.25c0 .69-.56 1.25-1.25 1.25h-4.5c-.69 0-1.25-.56-1.25-1.25v-4.5Z" />
              </svg>
            </h2>
            <p class="text-sm text-[var(--color-muted)]">{post.summary}</p>
            {post.tags && post.tags.length > 0 && (
              <div class="flex gap-2 mt-2">
                {post.tags.map(tag => (
                  <span class="text-xs px-2 py-0.5 bg-zinc-100 text-zinc-500 rounded-full">{tag}</span>
                ))}
              </div>
            )}
          </a>
        </li>
      ))}
    </ul>
  )}

</Layout>
